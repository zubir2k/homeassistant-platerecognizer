{
  "config": {
    "step": {
      "user": {
        "title": "Plate Recognizer — Setup",
        "description": "Enter your Plate Recognizer API token from {dashboard_url}. Enable **On-Premise** if you are running a local SDK instead of the cloud API.",
        "data": {
          "on_premise": "Use On-Premise SDK (local server)",
          "api_token": "API Token (leave blank for On-Premise)",
          "server": "On-Premise server URL (e.g. http://localhost:8080)",
          "camera_entity": "Source camera"
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid API token — check your Plate Recognizer dashboard.",
      "cannot_connect": "Could not connect to the API. Check your token or server URL.",
      "unknown": "Unexpected error. Check the Home Assistant logs."
    },
    "abort": {
      "already_configured": "This camera is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Plate Recognizer — Options",
        "data": {
          "regions": "Regions / countries to filter (comma-separated, e.g. my, sg)",
          "watched_plates": "Watched plates for fuzzy matching (comma-separated)",
          "detection_rule": "Detection rule",
          "region_mode": "Region strict mode",
          "mmc": "Enable Make / Model / Colour (paid plan)",
          "save_file_folder": "Folder to save processed images (leave blank to disable)",
          "save_timestamped_file": "Save timestamped image on detection",
          "always_save_latest_file": "Always save latest image (even with no detection)"
        }
      }
    }
  }
}
